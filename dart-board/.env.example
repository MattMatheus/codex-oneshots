# Container runtime configuration
DARTBOARD_DB_PATH=/data/dartboard.db

# USB capture is generally disabled in containerized mode on macOS Podman.
# Set true only if your runtime has camera passthrough configured.
DARTBOARD_CAPTURE_ENABLED=false
